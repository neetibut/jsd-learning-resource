# [Topic Name]

> ⚠️ **Stub** — This topic has a Mental Model draft but is not yet complete.
> Full content (code examples, exercises, links) coming soon.
> → [Back to Curriculum Map](../../README.md)

<!-- Remove the stub callout above when all 5 Done-when criteria are met -->

> **Before this:** You should have completed [Previous Module](../../XX-module/README.md) before starting this topic.

---

## The Mental Model

<!-- Done when: full prose, no bullet points, no code blocks, minimum 1 paragraph for stub, 2-3 for complete.
     Write like explaining to a smart adult over coffee. No jargon without definition. -->

[2-3 paragraphs. No bullet points. No code blocks. Write like you're explaining to a
smart adult over coffee who has never seen this concept before but is highly motivated
to understand it.]

---

## The Code

<!-- Done when: code is copy-pasteable and executes without modification.
     Must be tied to the spine project job board. No pseudocode. -->

```js
// One complete, runnable example tied to the spine project.
// Inline comments explain the non-obvious parts.
// This exact code should work when copied into the starter-code/ folder.
```

---

## Try It Yourself

<!-- Done when: exercise names a specific spine project deliverable and its week. -->

[One specific exercise tied to the spine project. Example:
"Add a `GET /jobs` route to your Express server that returns 5 hardcoded job objects
as JSON. This is the first real endpoint of the job board. (Week 6)"]

---

## Go Deeper

<!-- Done when: minimum 2 links, each with type, title, URL, and a curation note.
     Curation note is required — what to watch/read and what to skip. -->

- [VIDEO] Title — URL — curation note (e.g., "watch first 30 min; stop before MongoDB section")
- [READ] Title — URL — curation note (e.g., "focus on the Route parameters section only")

---

## The AI Age

<!-- Done when: all three sub-fields contain specific, non-generic content. -->

**When to use AI for this topic:** [what to delegate, be specific]

**What AI gets wrong:** [1-2 sentences, specific failure mode — not a generic warning.
Example: "Copilot skips input validation on `req.body`. Always add `express-validator`
to any route that accepts user data."]

**Try this prompt in Cursor:**
```
[Exact prompt, ready to copy-paste. Write for Cursor chat; also works in Claude.ai
with minimal adaptation. Default to public-audience wording.]
```

---

← [Previous: Topic Name](../relative-path/) | [Curriculum Map](../../README.md) | [Next: Topic Name →](../relative-path/)
